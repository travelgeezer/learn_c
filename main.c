#include <math.h>
#include <stdio.h>
/* main: generate some simple output */

int main(int argc, char *argv[])
{
  int x = 10;

  if (x > 0 && x < 10);
  else
    printf("x is out of range.\n");

  // change

  if (x <= 0 || x >= 10)
    {
      printf("x is out of range.\n");
    }


  return 0;
}
